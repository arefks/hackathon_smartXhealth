# code

This folder contains code and project scripts.

The Endo-FM dataset will be added here as a git submodule at `code/Endo-FM`.

To initialize or update the submodule:

```
# from repo root
git submodule update --init --recursive
```
